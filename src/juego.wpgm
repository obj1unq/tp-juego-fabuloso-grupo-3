import wollok.game.*
import objects.*

program juego{
	game.title("veremos")
	game.height(10)
	game.width(10)
	game.ground("suelo.png")
	
	game.addVisualCharacter(personaje)
	game.addVisual(proyectil)
	
	game.onCollideDo(personaje, {otro => otro.colisionoCon(personaje)})
	
	game.onTick(3000,"caeProyectil", {proyectil.caer()})
	
	game.start()
}
