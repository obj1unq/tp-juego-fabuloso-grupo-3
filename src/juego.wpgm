import wollok.game.*
import objects.*

program juego{
	game.title("veremos")
	game.height(10)
	game.width(10)
	game.ground("suelo.png")
	
	game.addVisualCharacter(personaje)
	
	//game.onCollideDo(proyectil, {jugador => jugador.colisionoCon(proyectil)})
	
	game.start()
}
